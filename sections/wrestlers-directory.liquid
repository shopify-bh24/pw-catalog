<div class="page-width">
  <h3>Debug Inspector</h3>
  {% for entry in metaobjects.collection_mapping.values limit: 1 %}
    <pre>{{ entry | json }}</pre>
  {% endfor %}
</div>

{% schema %}
{
  "name": "Wrestler Debugger",
  "settings": [],
  "presets": [{ "name": "Wrestler Debugger" }]
}
{% endschema %}