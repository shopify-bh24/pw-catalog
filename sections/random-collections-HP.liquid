<section class="page-width" style="padding: 50px; border: 5px solid red;">
  <h2>DIAGNOSTIC TEST: What is in 'wrestler_collection'?</h2>
  <div style="background: #eee; padding: 20px;">
    {% for entry in shop.metaobjects.collection_mapping.values %}
      <p>
        <strong>Entry:</strong> {{ entry.wrestler_name }}<br>
        <strong>Raw Value:</strong> {{ entry.wrestler_collection.value }}<br>
        <strong>Type of Value:</strong> {{ entry.wrestler_collection.value.class }}
      </p>
    {% endfor %}
  </div>
</section>

{% schema %}
{ "name": "Diagnostic", "presets": [{ "name": "Diagnostic" }] }
{% endschema %}