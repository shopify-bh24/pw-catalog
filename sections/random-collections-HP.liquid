{% comment %} 1. Shuffle the entries so we get a random selection every time {% endcomment %}
    {% assign shuffled_entries = shop.metaobjects.collection_mapping.values | shuffle %}
    {% assign count = 0 %}
    
    {% for entry in shuffled_entries %}
      {% comment %} 2. Use the text handle to look up the collection {% endcomment %}
      {% assign col_handle = entry.wrestler_collection.value | strip %}
      {% assign target_col = collections[col_handle] %}
      
      {% comment %} 3. Only display if the collection exists and we have fewer than 8 {% endcomment %}
      {% if target_col != blank and count < 8 %}
        <div class="rc-item">
          <a href="{{ target_col.url }}">
            {% if target_col.featured_image %}
              <img src="{{ target_col.featured_image | image_url: width: 400 }}" 
                   alt="{{ target_col.title | escape }}" 
                   loading="lazy">
            {% endif %}
            <h3>{{ target_col.title }}</h3>
          </a>
        </div>
        {% assign count = count | plus: 1 %}
      {% endif %}
    {% endfor %}